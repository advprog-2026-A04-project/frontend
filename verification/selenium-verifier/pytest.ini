[pytest]
addopts = -ra
pythonpath = .
markers =
    live: runs against deployed or local live services
    smoke: fast core smoke scenarios
    edge: higher-value edge-case scenarios
    admin: scenarios that exercise admin-only flows
